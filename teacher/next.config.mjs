/** @type {import('next').NextConfig} */
const nextConfig = {
  // The generation pipeline runs server-side via child_process (the existing
  // infra/pipeline CLI); nothing special needed here for the MVP.
};

export default nextConfig;
